#!/usr/bin/env bash
set -e
set -x

set -o allexport
. $SNAP/etc/opx/opx-environment
set +o allexport
case $1 in
    start)
        exec $SNAP/usr/bin/python $SNAP/usr/lib/opx/cps_db_stunnel_manager.py
        ;;
    *)
        echo "usage: $0 {start}"
        ;;
esac
exit 0
